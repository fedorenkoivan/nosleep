{
  "name": "nosleep-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd back-end && npm install && prisma generate && npm run build",
    "build:frontend": "cd front-end && npm install && npm run build"
  },
  "devDependencies": {
    "@types/node": "^25.3.2",
    "@vercel/node": "^3.0.0"
  },
  "dependencies": {
    "vercel": "^50.25.2"
  }
}
